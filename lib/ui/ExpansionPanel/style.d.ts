import { CSSProperties } from "react";
declare const style: {
    label: CSSProperties;
    inside: CSSProperties;
};
export declare const getLabelStyle: (style: CSSProperties) => {
    iconStyle: CSSProperties;
    labelStyle: CSSProperties;
};
export default style;
